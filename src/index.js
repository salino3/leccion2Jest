// npm i -D jest supertest
// npm i uuid

const app = require('./app');
// npx jest, and don't need to change anything in package.json

//*> in the terminal: npm test


app.listen(3200);
console.log("Server running on port 3200");







